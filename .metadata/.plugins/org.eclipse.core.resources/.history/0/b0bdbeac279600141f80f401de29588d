/*
 * PC01-2.c
 *
 *  Created on: Jan 6, 2015
 *      Author: Matthew Stephenson
 */

#include <stdio.h>
#include <stdlib.h>

int main(int *argc, char *argv[]) {
	switch(argv[1]){
	case '+':
		printf(atoi(argv[2]) + atoi(argv[3]));
		break;
	case '-':
		printf(atoi(argv[2]) - atoi(argv[3]));
		break;
	case '*':
		printf(atoi(argv[2]) * atoi(argv[3]));
		break;
	case '/':
		printf(atoi(argv[2]) / atoi(argv[3]));
		break;
	case '%':
		printf(atoi(argv[2]) % atoi(argv[3]));
		break;
	}
}
